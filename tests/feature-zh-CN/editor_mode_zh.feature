# language: zh-CN

@integration
功能: 在编辑器模式下编辑 Gherkin 步骤
  # 中等粒度：关注单一编辑器模式操作 — 进入编辑、选择关键字、插入/删除步骤和撤销。
  # 稳定契约：`explore_enter_edit`、`begin_step_or_title_edit`、`step_keyword_picker`、
  #   `insert_step`（Tab 键）、`delete_current_node`（dd）、`undo`（Ctrl+/）、
  #   `bdd_nav::step_edit_start_col`、`keymap::Action::SwitchKeyword`

  背景:
    假如 TUI 已运行并加载了包含以下内容的功能文件:
      """
      功能: 用户登录
        场景: 成功登录
          假如 我在登录页面
          当 我输入我的用户名
      """
      并且 Explore 标签页已激活，光标位于"成功登录"场景的
          "假如 我在登录页面"步骤上

  场景: 进入步骤编辑模式并修改步骤文本
    当 我按下 `e` 进入编辑器
      并且 光标位于"假如 我在登录页面"行
      并且 我按下 `Space`（焦点在主体区域）
    那么 该行应进入步骤输入模式（step_input_active）
      并且 我应能在行尾追加或修改文本
    当 我按下 `Esc`
    那么 步骤输入模式应退出，返回导航模式

  场景: 使用关键字选择器切换步骤关键字
    假如 我已进入编辑器，光标位于"假如 我在登录页面"行
      并且 焦点在关键字槽上（focus_slot = Keyword）
    当 我按下 `Space`
    那么 步骤关键字选择器应出现（step_keyword_picker）
      并且 选择器应包含"假如"、"当"、"那么"、"并且"和"但是"选项
    当 我使用 `↑`/`↓` 移动到"当"并按下 `Enter`
    那么 该行的关键字应改为"当"
      并且 关键字选择器应关闭

  场景: 使用 Tab 在当前步骤下方插入新步骤
    假如 我已进入编辑器，光标位于"假如 我在登录页面"行
    当 我按下 `Tab`
    那么 应在当前行下方出现一行新的空步骤行，带有默认关键字
      并且 新步骤的缩进应与当前步骤一致

  场景: 删除当前步骤
    假如 我已进入编辑器，光标位于"假如 我在登录页面"行
    当 我依次按下 `d` 再按 `d`
    那么 该步骤行应被删除
      并且 场景中剩余的步骤应上移
    当 我按下 `Ctrl+/`（撤销）
    那么 被删除的步骤行应恢复

  场景: 保存更改并退出
    假如 我已进入编辑器并修改了步骤文本
    当 我按下 `s`
    那么 底部状态栏应短暂显示保存成功消息
      并且 磁盘上的功能文件应包含修改后的步骤文本
    当 我按下 `q`
    那么 应出现确认提示（因为缓冲区被标记为已修改）
    当 我再次按下 `q`
    那么 TUI 应退出
