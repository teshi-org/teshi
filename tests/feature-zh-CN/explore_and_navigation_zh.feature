# language: zh-CN

@e2e
功能: 从终端启动并浏览 BDD 项目
  # 粗粒度：涵盖启动 TUI → 加载 .feature 文件 → 三栏导航 → 切换标签页的完整用户旅程。
  # 稳定契约：CLI `teshi <dir>`、`App::from_directory`、Explore 三栏渲染
  #   （ui.rs render_explore_panel）、`MainTab` 标签页切换（`handle_action SelectTab`）、
  #   键盘映射（keymap.rs Action::MoveUp/MoveDown/TreeUp 等）

  背景:
    假如 当前工作目录至少包含两个有效的 .feature 文件
      并且 其中一个文件包含三个场景，另一个文件包含一个场景

  场景: 打开目录并浏览功能列表
    当 我在终端中运行 `teshi .`
    那么 TUI 应启动并显示 Explore 标签页
      并且 左侧的"功能"列应列出所有 .feature 文件名
      并且 第一个文件应默认高亮

  场景: 使用键盘在场景列和步骤列之间导航
    假如 TUI 正在 Explore 标签页运行
      并且 在左侧功能列中选中了一个功能文件
    当 我按下 `→` 或 `Tab`
    那么 焦点应移到"场景"列
      并且 该列应列出所选功能文件中的所有场景名
    当 我再次按下 `→` 或 `Tab`
    那么 焦点应移到"步骤"列
      并且 该列应列出所选场景中的所有步骤文本
    当 我按下 `←` 或 `BackTab`
    那么 焦点应返回到前一列

  场景: 切换到思维导图标签页并查看步骤树
    假如 TUI 正在 Explore 标签页运行
    当 我按下 `2` 键
    那么 活动标签页应切换到"思维导图"
      并且 左侧应显示一个可折叠的树，以项目目录名为根节点
      并且 展开的节点应显示功能文件名作为第一级子节点
      并且 展开功能节点应显示场景名及其所有步骤

  场景: 切换到帮助标签页并查看键盘绑定参考
    假如 TUI 正在运行
    当 我按下 `4` 键
    那么 活动标签页应切换到"帮助"
      并且 内容区域应显示键盘绑定参考文本，包含"标签页"、"思维导图"和"浏览"等章节
