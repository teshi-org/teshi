# language: zh-CN

@integration
功能: LLM 智能体辅助编辑
  # 中等粒度：关注完整的 LLM 工具调用流程 — 用户发送消息 → 智能体调用工具 → 变更排队等待确认 →
  #   用户接受/拒绝 → 应用到缓冲区。
  # 稳定契约：`agent::execute_tool` 分发、`AgentPendingChange` 队列、
  #   `queue_agent_change` / `accept_agent_change` / `reject_agent_change`、
  #   TUI agent_change_prompt 渲染（"[Y] 接受 [N] 拒绝 [Esc] 拒绝"）、
  #   `agent::tools::get_tools` 六个工具的注册、
  #   `AgentThread` 每个智能体状态、侧边栏渲染含状态图标

  背景:
    假如 环境变量 TESHI_LLM_API_KEY 已设置为有效的 API 密钥
      并且 TUI 已运行并加载了包含功能文件的项目
      并且 我已按下 `3` 切换到 AI 标签页

  场景: 向 LLM 发送消息并接收文本回复
    当 我在 AI 输入框中输入"告诉我关于当前项目的信息"
      并且 按下 `Enter` 发送消息
    那么 我的用户消息应出现在聊天历史中
      并且 AI 状态应变为"等待中"
      并且 稍等片刻后，一条助手回复消息应追加到聊天历史中
      并且 回复应包含基本信息，如项目目录和功能文件数量

  场景: LLM 通过 insert_scenario 工具请求插入新场景
    假如 AI 标签页中有一个活跃的对话
    当 我发送消息"请在 login.feature 中添加一个名为'密码重置'的场景，
        步骤为：假如 一个已登录的用户，当 我点击忘记密码"
    那么 聊天历史应显示 LLM 调用了 `insert_scenario` 工具
      并且 底部状态栏应显示"AI 想要修改文件"的确认提示
      并且 提示应包含 `[Y] 接受`、`[N] 拒绝` 和 `[Esc] 拒绝` 选项
    当 我按下 `Y` 接受更改
    那么 底部状态栏应显示确认成功消息
      并且 login.feature 的编辑器缓冲区应包含新插入的
          "场景: 密码重置"块及其步骤

  场景: 用户拒绝 LLM 文件修改请求
    假如 AI 标签页中有一个活跃的对话
      并且 LLM 刚刚通过 `update_step` 工具请求了步骤修改
      并且 底部栏显示了"AI 想要修改文件"的确认提示
    当 我按下 `N`
    那么 更改应被丢弃，缓冲区应保持不变
      并且 底部状态栏应显示拒绝确认消息
      并且 LLM 应收到指示更改已被用户拒绝的工具结果反馈

  场景: LLM 调用 highlight_mindmap_nodes 高亮思维导图节点
    假如 AI 标签页中有一个活跃的对话
    当 我发送消息"将所有包含'login'的思维导图节点高亮为蓝色"
    那么 聊天历史应显示 LLM 调用了 `highlight_mindmap_nodes` 工具
      并且 当 我切换到思维导图标签页（按 `2`）
      所有步骤文本包含"login"（不区分大小写）的树节点应高亮为蓝色

  场景: 未配置 LLM 时 AI 标签页隐藏
    假如 环境变量 TESHI_LLM_API_KEY 未设置或为空
      并且 TUI 正在运行
    那么 顶部标签栏不应显示"AI [3]"选项
      并且 按下 `3` 应无效

  场景: 创建并在多个智能体之间切换
    假如 AI 标签页已激活，智能体 1 被选中
    当 我按下 `a` 创建新智能体
    那么 一个新的智能体 2 应出现在左侧侧边栏
      并且 主面板应显示智能体 2 的空白对话
      并且 智能体 2 的状态图标应显示 `○`（空闲）
    当 我在智能体 2 中输入消息并按下 `Enter`
    那么 智能体 2 的状态图标应变为 `●`（等待中）
    当 我按下 `k` 切换到智能体 1
    那么 主面板应显示智能体 1 的对话
      并且 智能体 2 的状态图标应在侧边栏中保持 `●`（等待中）
    当 我按下 `j` 切换回智能体 2
    那么 主面板应再次显示智能体 2 的对话

  场景: 关闭智能体
    假如 AI 标签页已激活且至少有两个智能体
    当 我按下 `x` 关闭选中的智能体
    那么 该智能体应从侧边栏中移除
      并且 焦点应切换到剩余的智能体
    当 我按下 `x` 且仅剩一个智能体
    那么 状态消息应显示"无法关闭最后一个智能体"

  场景: 智能体在文件更改排队后显示 AwaitingApproval 状态
    假如 AI 标签页中有一个活跃的对话
    当 我发送一条导致 LLM 调用变更工具的消息
    那么 智能体的状态应变为 `AwaitingApproval`
      并且 侧边栏应为该智能体显示 `◆`（青色）
      并且 底部应出现 Y/N 确认提示
    当 我按下 `Y` 接受
    那么 智能体的状态应返回 `Waiting` 或 `Idle`
      并且 侧边栏图标应相应更新

  场景: AI 聊天内容自动换行，无水平滚动条
    假如 AI 标签页中有一个活跃的对话
    当 AI 发送的回复中包含比面板宽度更长的行
    那么 文本应自动换行到下一行
      并且 聊天区域不应出现水平滚动条
