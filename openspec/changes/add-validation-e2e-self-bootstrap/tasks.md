## 1. Validation E2E runner

- [x] 1.1 Add `teshi-validation-cli-runner` as a workspace test binary using the existing NDJSON run protocol.
- [x] 1.2 Implement an isolated scenario world that launches `TESHI_BIN`, captures status/stdout/stderr, writes fixtures, and owns temporary state.
- [x] 1.3 Implement English and Chinese step definitions for check, binding, run, and replay gate assertions.

## 2. Executable Features and integration harness

- [x] 2.1 Add paired `@cli` validation E2E Features with concrete, selector-free scenarios.
- [x] 2.2 Add `apps/teshi-cli/tests/validation_cli_bdd.rs` to build the runner, copy only validation-tagged Features, pass the exact target binary, and assert per-locale pass/fail counts.
- [x] 2.3 Ensure malformed fixture source is generated by the runner rather than embedded as an invalid executable Feature.

## 3. Developer and CI workflow

- [x] 3.1 Document the self-bootstrap command, isolation model, and the boundary between black-box E2E and Rust-level parser/editor tests.
- [x] 3.2 Add the focused validation E2E command to CI while retaining existing `teshi check --all` and Rust gates.

## 4. Verification

- [x] 4.1 Run both locale E2E suites and inspect `end_run` plus process exit status.
- [x] 4.2 Run formatting, native workspace tests, strict Clippy, docs, and OpenSpec validation.
