# AGENTS.md

## Cursor Cloud specific instructions

This repository (`teshi`) is currently an empty scaffold with only a `README.md` and `LICENSE` (MIT) file. There is no application code, no dependency manifests, no build configuration, and no services.

- **No dependencies** to install (no `package.json`, `requirements.txt`, `pyproject.toml`, etc.)
- **No build/lint/test commands** available yet
- **No services** to start or test

When application code is added, update this section with the relevant development workflow (install, lint, test, build, run commands).
